[project]
name = "dove"
version = "0.1.0"
description = ""
authors = [{ name = "Julio Formiga", email = "julio.formiga@gmail.com" }]
license = { text = "MIT" }
dependencies = [
  "rich>=13.6.0",
  "pyinstaller>=6.1.0",
  "requests>=2.32.3",
  "textual>=0.65.1",
]
requires-python = ">=3.8.1"

[tool.pyright]
venvPath = "."
venv = ".venv"

[tool.pdm]
distribution = false
